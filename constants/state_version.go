package constants

import "github.com/datazip-inc/olake/lib/constants"

const (
	LatestStateVersion = constants.LatestStateVersion
)

// Used as the current version of the state when the program is running
var LoadedStateVersion = LatestStateVersion
